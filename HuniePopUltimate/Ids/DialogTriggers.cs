using Hp2BaseMod;

namespace HuniePopUltimate;

public static class DialogTriggers
{
    public static readonly RelativeId PreBedroom = new RelativeId(Plugin.ModId, 0);
    public static readonly RelativeId PreSex = new RelativeId(Plugin.ModId, 1);
    public static readonly RelativeId PostSex = new RelativeId(Plugin.ModId, 2);
}